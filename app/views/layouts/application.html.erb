<!DOCTYPE html>
<html>
  <head>
    <title>MediaRanker</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <header>
      <h1>Media Ranker</h1>
      <p>Ranking the Best of Everything</p>

      <nav>
        <span>View Top Media</span>
        <span>View All Media</span>
        <span>Add A New Work</span>
        <span>View All Users</span>

        <% current_user = User.find_by(id: session[:user_id]) %>
        <% if current_user %>
          <span>Logged in as <%= link_to current_user.username, current_user_path %></span>
          <span><%= link_to "Log Out", logout_path, method: :post%></span>
        <% else %>
          <span><%= link_to "Log In", login_path %></span>
        <% end %>
      </nav>
    </header>
    
    <main>
      <section class="flash">
        <% flash.each do |name, message| %>
          <div><%= name %> : <%= message %></div>
        <% end %>
      </section>

      <%= yield %>
    </main>
  </body>
</html>
